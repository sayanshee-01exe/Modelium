# Core
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
imbalanced-learn>=0.12
pyarrow>=15.0

# Modelling
xgboost>=2.0
lightgbm>=4.3
catboost>=1.2
optuna>=3.6

# Tracking / orchestration
mlflow>=2.14
dvc>=3.50
# Imported directly by src/utils/config_loader.py to read params.yaml. It arrives
# transitively via dvc, but a direct import deserves a direct declaration — otherwise
# dropping dvc would silently break config loading.
pyyaml>=6.0

# Explainability
shap>=0.45

# Visualisation / EDA
matplotlib>=3.8
seaborn>=0.13
missingno>=0.5

# Serialisation
joblib>=1.3

# Testing
pytest>=8.0

# Security tooling (the /dev workflow runs a dependency audit per task)
pip-audit>=2.7

# Known-accepted transitive advisories, re-checked each /dev run:
#   cryptography 49.x  PYSEC-2026-3552 — PKCS#7/S-MIME decryption oracle. Fixed in 50.0.0,
#     but mlflow pins cryptography<50, so requiring the fix makes the install unresolvable.
#     Unreachable here: nothing in this repo calls pkcs7_decrypt_*; it arrives only via
#     mlflow / google-auth / asyncssh. Revisit when mlflow relaxes the pin.
#   diskcache 5.6.3    PYSEC-2026-2447 — pickle deserialization via the cache dir. 5.6.3 is
#     the newest release; no upstream fix exists. Requires write access to the local DVC
#     cache, which already implies local code execution.

# --- API serving (Step 10) -----------------------------------------------------------
# Local FastAPI service for the registered champion. uvicorn is the ASGI server;
# httpx backs fastapi.testclient.TestClient, so it is a test dependency in practice.
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27
