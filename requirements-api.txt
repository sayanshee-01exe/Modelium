# Runtime dependencies for the serving image only.
#
# requirements.txt is the *development* environment: it carries the training stack (DVC,
# Optuna, CatBoost), the plotting stack and the test tooling, none of which a serving
# container executes. Installing it produced a 4.3 GB image whose three largest entries —
# nvidia (291 MB), catboost (269 MB) and plotly (67 MB) — are never imported by the API.
#
# Kept deliberately, despite the size:
#   xgboost  the API must not hard-code LightGBM; a champion may be an XGBoost model and
#            the image should serve it without a rebuild
#   shap     powers POST /explain (it pulls numba/llvmlite, which is most of its weight)
#   mlflow   resolves the champion through the registry alias
#
# Keep this in step with requirements.txt when a serving dependency changes.

# --- API ---
fastapi>=0.115
uvicorn>=0.30

# --- data handling ---
pandas>=2.0
numpy>=1.26
pyarrow>=15.0
pyyaml>=6.0
joblib>=1.3

# --- model + registry ---
scikit-learn>=1.4
lightgbm>=4.3
xgboost>=2.0
mlflow>=2.14

# --- explanation ---
shap>=0.45
matplotlib>=3.8
